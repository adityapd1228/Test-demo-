'use client';export default function Tabs({tabs,current,onChange}:{tabs:string[];current:string;onChange:(t:string)=>void}){return <div className='flex gap-2 border-b border-[var(--line)] mb-3'>{tabs.map(t=>(<button key={t} onClick={()=>onChange(t)} className={`px-3 py-2 rounded-t ${current===t?'bg-[var(--card)] border border-b-0 border-[var(--line)]':'text-slate-300'}`}>{t}</button>))}</div>};